<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RakshaKavach">

        <!-- Splash Activity -->
        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.RakshaKavach.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity -->
        <activity
            android:name=".ui.home.MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Profile Activity -->
        <activity
            android:name=".ui.profile.ProfileActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Checklist Activity (Task Selector) -->
        <activity
            android:name=".ui.checklist.ChecklistActivity"
            android:exported="false" />

        <!-- Gear Checklist Activity (opens after task selection) -->
        <activity
            android:name=".ui.checklist.GearChecklistActivity"
            android:exported="false" />

        <!-- Risk Meter Activity -->
        <activity
            android:name=".ui.riskmeter.RiskMeterActivity"
            android:exported="false" />

        <!-- Incident Log Activity -->
        <activity
            android:name=".ui.incident.IncidentLogActivity"
            android:exported="false" />

        <!-- Safety Quiz Activity -->
        <activity
            android:name=".ui.quiz.SafetyQuizActivity"
            android:exported="false" />

        <!-- Safety Score Activity -->
        <activity
            android:name=".ui.score.SafetyScoreActivity"
            android:exported="false" />

        <!-- Login Activity -->
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Register Activity -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Notification Worker -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
